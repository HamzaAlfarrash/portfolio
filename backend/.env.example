# Backend Environment Variables
# CORS origins can be set here if needed (comma-separated)
# CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Debug mode (set to true for development)
DEBUG=false
